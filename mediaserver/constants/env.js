const SERVER_PORT = process.env.SERVER_PORT;
const AWS_REGION = process.env.AWS_REGION;
const AWS_BUCKET_NAME = process.env.AWS_BUCKET_NAME;
const AWS_BUCKET_SERVER = process.env.AWS_BUCKET_SERVER;

module.exports = {
  SERVER_PORT,
  AWS_BUCKET_NAME,
  AWS_REGION,
  AWS_BUCKET_SERVER,
};
